export type PaginationType = {
  count: number;
  next: string;
  previous: string;
};

export type MediaBannerType = {
  media_id: string;
  media_name: string;
  media_type: string;
  media_length: string;
  media_pre_url: string;
};
